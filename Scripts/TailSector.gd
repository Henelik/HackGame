extends Area2D

# Declare member variables here.
var tileX
var tileY

func setColor(col):
	$Sprite.modulate = col

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
